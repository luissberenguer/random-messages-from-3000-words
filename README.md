# random-messages-from-3000-words

To instal the package just type:
`npm install random-messages-from-3000-words`

Then add:

```javascript
const randomMsg = require('random-messages-from-3000-words');
```

And use:

```javascript
randomMsg.randomMsg();
```
